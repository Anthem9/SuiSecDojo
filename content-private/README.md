# Private Content Workspace

This directory is intentionally ignored by git.

Use `content-private/answers/` for plaintext answer drafts before Seal encryption.
Only encrypted outputs should be copied into deployable public assets.
