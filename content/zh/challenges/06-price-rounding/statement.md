# Challenge 06：价格舍入

## 背景

DeFi 计算经常涉及整数除法、精度缩放和舍入方向。错误的舍入策略会让小额多次操作获得不成比例的收益。

## 漏洞

本题用一个简化购买模型展示价格舍入问题：每次购买都按错误方向取整，用户可以通过多次小额调用累积超额积分。

## 目标

领取实例，选择合适的购买次数和金额，让积分超过阈值，然后提交 solve。

## CLI / PTB 练习

重点观察 `payment`、`buy_repeats` 和最终 `credits`。不要只点按钮，先估算一次调用和多次调用的差异。
