# Challenge 05：错误初始化

## 背景

初始化路径决定了管理员能力、全局配置和关键状态的创建者。错误的初始化设计会让普通用户制造本不该存在的权限对象。

## 漏洞

本题把创建管理员 capability 的入口暴露给了普通用户。用户可以为自己的实例创建非法 `AdminCap`，再用它修改初始化状态。

## 目标

领取实例，通过公开入口创建管理员 capability，执行初始化动作，然后提交 solve。

## CLI / PTB 练习

先读对象字段，确认哪些状态只有管理员应能改动。挑战模式下请自己构造创建 cap、使用 cap、solve 三步交易。
