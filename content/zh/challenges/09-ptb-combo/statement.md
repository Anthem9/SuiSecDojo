# Challenge 09：PTB 原子组合

## 背景

Sui 的 Programmable Transaction Block 可以在同一笔交易中组合多个调用。合约如果假设两个动作不会原子连续发生，就可能出现状态假设失效。

## 漏洞

本题要求把几个步骤放进同一个 PTB。分开交易时状态会被重置或不满足条件，但同一 PTB 中可以连续完成。

## 目标

领取实例，按正确顺序构造 PTB，让实例进入可 solve 状态，然后提交 solve。

## CLI / PTB 练习

重点不是单个参数，而是调用顺序和是否处在同一笔交易里。
