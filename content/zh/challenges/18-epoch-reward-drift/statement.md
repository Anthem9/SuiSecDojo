# Challenge 18：Epoch 奖励漂移

## 背景

奖励发放通常依赖 epoch、时间窗口或累计指数。边界处理错误会让用户重复领取或跨周期多拿奖励。

## 漏洞

本题的奖励累计逻辑没有正确更新边界状态。用户可以利用 epoch 参数或重复调用，让 rewards 超过预期。

## 目标

领取实例，选择合适的 epoch 和重复次数，让奖励达到 solve 阈值，然后提交 solve。

## CLI / PTB 练习

观察 `last_epoch` 和 `rewards` 的变化，尤其是重复调用时状态是否正确推进。
