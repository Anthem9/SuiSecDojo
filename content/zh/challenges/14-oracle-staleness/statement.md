# Challenge 14：预言机过期价格

## 背景

价格数据不仅要正确，还要足够新。只验证价格来源，不验证 epoch 或时间戳，会产生过期价格风险。

## 漏洞

本题允许调用者使用旧 epoch 的价格数据。过期价格仍能通过检查，并触发本不该发生的状态变更。

## 目标

领取实例，选择能体现过期问题的 epoch 参数，使用 stale price 后提交 solve。

## CLI / PTB 练习

关注 `checked_epoch` 和 `price_epoch` 的关系。安全逻辑应同时验证来源和新鲜度。
