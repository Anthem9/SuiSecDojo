# Challenge 07：错误的边界检查

## 背景

Move 默认会防止整数溢出，但业务代码仍可能在错误位置做检查，导致边界输入绕过预期约束。

## 漏洞

本题的检查逻辑验证了错误变量或错误阶段。攻击者可以提交边界值，让实例进入普通路径无法达到的状态。

## 目标

领取实例，找到能通过检查并改变受保护状态的数值，然后提交 solve。

## CLI / PTB 练习

先读函数签名和 abort 条件，再尝试边界输入。挑战模式下请自己填入关键数值。
