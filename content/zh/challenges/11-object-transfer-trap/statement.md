# Challenge 11：对象转移陷阱

## 背景

对象所有权和转移路径是 Sui 安全模型的核心。错误地把对象交给不该持有的人，会让后续权限判断失效。

## 漏洞

本题模拟一个 custody 流程：实例把关键对象或控制权错误地交给调用者，导致调用者可以完成后续受限动作。

## 目标

领取实例，触发错误 custody 路径，拿到足够控制权后提交 solve。

## CLI / PTB 练习

观察对象 owner 是否变化，以及哪个入口允许你接受或接管 custody。
