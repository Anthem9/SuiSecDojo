# Challenge 16：签名者混淆

## 背景

链上权限应以 `tx_context::sender` 和真实对象所有权为准。调用者传入的地址或意图字段不能直接当作签名者。

## 漏洞

本题错误信任调用者提供的 signer/intent。用户可以填入看似可信的地址，让合约接受本不该接受的意图。

## 目标

领取实例，构造 `claimed_signer` 参数，使 intent 被接受，然后提交 solve。

## CLI / PTB 练习

比较参数里的地址和真实交易发送者。安全代码应验证 sender，而不是只验证传入字段。
