# Challenge 13：委托 Capability 滥用

## 背景

委托权限可以降低操作门槛，但如果没有限制范围、有效期或用途，会让临时权限变成长期管理员权限。

## 漏洞

本题把 capability 委托给了调用者，却没有正确限制它能执行的动作。用户可以用被委托的 cap 设置受保护标记。

## 目标

领取实例，获取委托 capability，使用它完成受限操作，然后提交 solve。

## CLI / PTB 练习

观察 capability 是否绑定实例、是否检查用途、是否能重复使用。
