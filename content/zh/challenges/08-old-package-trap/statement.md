# Challenge 08：旧入口陷阱

## 背景

合约升级后，旧入口或旧路径如果仍可调用，可能绕过新版本里的修复逻辑。

## 漏洞

本题用简化的 old/new path 模型模拟升级风险：新路径做了检查，但旧路径仍能修改关键状态。

## 目标

领取实例，比较新旧路径，使用仍然有问题的旧路径改变状态，然后提交 solve。

## CLI / PTB 练习

不要只看“新函数是否安全”，还要确认旧函数是否仍公开、是否还能写状态。
