# Challenge 20：清算边界条件

## 背景

借贷清算依赖抵押率、债务、价格和阈值。边界值和舍入方向错误会让健康仓位被误清算，或让坏账无法清算。

## 漏洞

本题用简化 health factor 模型展示边界错误。特定价格和阈值会把实例推入错误清算状态。

## 目标

领取实例，选择 `price` 和 `threshold`，让 health 落入漏洞边界并触发 liquidation，然后提交 solve。

## CLI / PTB 练习

先计算健康值，再执行交易。重点是阈值比较使用 `<`、`<=` 和整数舍入时的差异。
