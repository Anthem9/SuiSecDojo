# Source Notes: Shared Object Pollution

Key module: `challenge_12_shared_object_pollution`.

Important entries:

- `claim(progress, ctx)`
- `vulnerable_pollute(instance)`
- `solve(instance, progress, mode, assistance_level, ctx)`
