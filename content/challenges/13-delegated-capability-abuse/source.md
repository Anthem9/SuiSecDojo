# Source Notes: Delegated Capability Abuse

Key module: `challenge_13_delegated_capability_abuse`.

Important entries:

- `vulnerable_delegate_cap(instance, ctx)`
- `privileged_set_flag(instance, cap)`
- `solve(instance, progress, mode, assistance_level, ctx)`
