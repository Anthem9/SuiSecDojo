# Source: Shared Vault

Primary module:

```text
contracts/sources/challenge_02_shared_vault.move
```

Primary tests:

```text
contracts/tests/challenge_02_tests.move
```

Key entries:

- `claim(progress, ctx)`
- `vulnerable_withdraw(vault, amount)`
- `solve(instance, vault, progress, ctx)`
