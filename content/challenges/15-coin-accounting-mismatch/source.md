# Source Notes: Coin Accounting Mismatch

Key module: `challenge_15_coin_accounting_mismatch`.

Important entries:

- `deposit(instance, amount)`
- `vulnerable_credit_without_coin(instance, amount)`
- `solve(instance, progress, mode, assistance_level, ctx)`
