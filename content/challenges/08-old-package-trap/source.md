# Source Notes: Old Package Trap

Core entries:

```move
challenge_08_old_package_trap::claim(progress, ctx)
challenge_08_old_package_trap::new_checked_path(instance)
challenge_08_old_package_trap::old_vulnerable_path(instance)
challenge_08_old_package_trap::solve(instance, progress, ctx)
```

The exploit uses the old path, not the fixed path.
