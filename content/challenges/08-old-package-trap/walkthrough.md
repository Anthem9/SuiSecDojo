# Walkthrough: Old Package Trap

`new_checked_path` represents the fixed entry. It rejects the unsafe action.

`old_vulnerable_path` remains callable and sets the same protected condition needed for solve.

Solve succeeds when `legacy_flag` is true.
