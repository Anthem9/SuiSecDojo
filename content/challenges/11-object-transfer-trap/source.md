# Source Notes: Object Transfer Trap

Key module: `challenge_11_object_transfer_trap`.

Important entries:

- `claim(progress, ctx)`
- `vulnerable_accept_custody(instance, ctx)`
- `solve(instance, progress, mode, assistance_level, ctx)`

This is a minimal education model. It is not a real protocol exploit.
